"""AI Evaluation Engine — modular pipeline for scoring interview performance.

All AI output passes through validation before any data is persisted.
The LLM never writes directly to the database.
"""
